/**
 * 包含实体效果的其他选项。
 */
export interface EntityEffectOptions {
    /**
     * @remarks
     * 效果的强度。
     *
     */
    amplifier?: number;
    /**
     * @remarks
     * 如果为 true，则在实体上有效果时显示粒子。
     *
     */
    showParticles?: boolean;
}