/**
 * 包含实体效果的附加选项。
 */
export interface EntityEffectOptions {
    /**
     * @remarks
     * 效果的强度。
     *
     */
    amplifier?: number;
    /**
     * @remarks
     * 如果为 true，则当效果在实体上时显示粒子。
     *
     */
    showParticles?: boolean;
}