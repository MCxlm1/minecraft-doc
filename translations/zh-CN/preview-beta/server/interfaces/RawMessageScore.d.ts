/**
 * 提供用于原始消息中的分数令牌的描述。
 */
export interface RawMessageScore {
    /**
     * @remarks
     * 要匹配的分数值的名称。
     *
     */
    name?: string;
    /**
     * @remarks
     * 要匹配的分数值的名称。
     *
     */
    objective?: string;
}