/**
 * 用于指定目标及其参与者列表的显示排序方式。
 */
export enum ObjectiveSortOrder {
    /**
     * @remarks
     * 目标参与者列表按升序（例如 A-Z）显示。
     *
     */
    Ascending = 0,
    /**
     * @remarks
     * 目标参与者列表按降序（例如 Z-A）显示。
     *
     */
    Descending = 1,
}