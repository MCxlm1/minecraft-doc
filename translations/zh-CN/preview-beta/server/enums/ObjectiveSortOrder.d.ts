/**
 * 用于指定显示目标和其参与者列表的排序顺序。
 */
export enum ObjectiveSortOrder {
    /**
     * @remarks
     * 目标参与者列表按升序显示（例如，A-Z）。
     *
     */
    Ascending = 0,
    /**
     * @remarks
     * 目标参与者列表按降序显示（例如，Z-A）。
     *
     */
    Descending = 1,
}