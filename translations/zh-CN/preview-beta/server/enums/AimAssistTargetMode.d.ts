/**
 * 指定用于瞄准辅助的不同目标模式。
 */
export enum AimAssistTargetMode {
    /**
     * @remarks
     * 基于角度的目标选择。
     *
     */
    Angle = 'Angle',
    /**
     * @remarks
     * 基于距离的目标选择。
     *
     */
    Distance = 'Distance',
}